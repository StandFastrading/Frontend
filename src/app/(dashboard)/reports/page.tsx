import { ReportsPage } from "@/features/reports/reports-page";

// Reports route — Performance × Behavior synthesis. Pure composition; all
// computation lives in `features/reports/reports-engine.ts`.

export default function Page() {
  return <ReportsPage />;
}
